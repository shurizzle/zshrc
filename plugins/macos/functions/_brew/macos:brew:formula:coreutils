macos:brew:add-gnubin-in-path coreutils
macos:brew:add-manpath coreutils

# stty doesn't work good on macOS
function stty {
  /bin/stty "$@"
}

# vim: ft=zsh sts=2 ts=2 sw=2 et fdm=marker fmr={{{,}}}
