local prefix
zstyle -s ':zoppo:plugin:macos:brew' prefix prefix

if (( $+1 )); then
    local res="${prefix}/${1##/#}"
    if [[ -e "$res" ]]; then
        print -n -- "$res"(:P)
    else
        print -n -- "$res"
    fi
else
    print -n -- "$prefix"
fi
