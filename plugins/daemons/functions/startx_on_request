local startx
echo -n "Do you want to start X11? (Y/n) "
read -t 3 startx
case "${startx}" in
  ([Nn]|[Nn][Oo])
  ;;
  (*)
    startx
  ;;
esac

# vim: ft=zsh sts=2 ts=2 sw=2 et fdm=marker fmr={{{,}}}
